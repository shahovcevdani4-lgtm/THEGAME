"""Data package exposing game configuration modules."""

# Explicit package to support execution environments that do not
# automatically treat bare directories as namespace packages (e.g. when the
# script is launched via file associations on Windows).

